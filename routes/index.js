const express = require('express');
const router = express.Router();
/*IMPORTAR LOS CONTROLLERS DE SUS RUTAS
example: 
const userController = require('../controllers/userController');
*/

module.exports = () => {
    /* Rutas o endpoints (get, post, put, delete) */
    

    return router;
}